# GCN-MPPR

install required packages

```
pip install -r requirements.txt
```

unzip two files
```
unzip .\data\amazon_electronics_computers.zip -d data\
unzip .\ppnp\dataset_for_paper\amazon_electronics_computers.zip -d .\ppnp\dataset_for_paper
```
run the main code for node classification

```
!python Main.py
```

Full experimental codes would be open source after acceptance. 
